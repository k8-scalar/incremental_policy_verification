
kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: red-to-green
  namespace: test
spec:
  podSelector:
    matchLabels:
      color: red
  policyTypes:
    - Egress
  egress:
  - to:
    - podSelector:
        matchLabels:
          color: green
    ports:
      - port: 80
