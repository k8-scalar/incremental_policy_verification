kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: green-to-blue
  namespace: test
spec:
  podSelector:
    matchLabels:
      color: green
  policyTypes:
    - Egress
  egress:
  - to:
    - podSelector:
        matchLabels:
          color: blue
    ports:
      - port: 80
