kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: blue-from-red
  namespace: test
spec:
  podSelector:
    matchLabels:
      color: blue
  policyTypes:
    - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          color: red
    ports:
      - port: 80
