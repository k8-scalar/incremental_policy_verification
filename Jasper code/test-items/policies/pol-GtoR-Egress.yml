kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: green-to-red
  namespace: test
spec:
  podSelector:
    matchLabels:
      color: green
  policyTypes:
    - Egress
  egress:
  - to:
    - podSelector:
        matchLabels:
          color: red
    ports:
      - port: 80
