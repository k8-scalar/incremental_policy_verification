kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: blue-to-green
  namespace: test
spec:
  podSelector:
    matchLabels:
      color: blue
  policyTypes:
    - Egress
  egress:
  - to:
    - podSelector:
        matchLabels:
          color: green
    ports:
      - port: 80
