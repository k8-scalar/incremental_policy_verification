kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: red-to-red
  namespace: test
spec:
  podSelector:
    matchLabels:
      color: red
  policyTypes:
    - Egress
  egress:
  - to:
    - podSelector:
        matchLabels:
          color: red
    ports:
      - port: 80
