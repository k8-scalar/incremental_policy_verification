kind: NetworkPolicy
apiVersion: networking.k8s.io/v1
metadata:
  name: red-from-blue
  namespace: test
spec:
  podSelector:
    matchLabels:
      color: red
  policyTypes:
    - Ingress
  ingress:
  - from:
    - podSelector:
        matchLabels:
          color: blue
    ports:
      - port: 80
